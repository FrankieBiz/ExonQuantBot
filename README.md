# ExonQuantBot
An automated, news‑driven quant trading bot that uses an independent RSS scraper with VADER sentiment, stored in SQLite and exposed via FastAPI. The bot pulls recent articles, aggregates sentiment, and triggers trades through a broker API (e.g., Interactive Brokers) based on predefined rules.
